# PPT 生成待办事项

## 1. 文字+文件输入场景支持

**现状：** 当前 PPT 生成主要基于单一输入源（纯文字或纯文件）

**目标：** 支持用户同时提供文字描述 + 文件（PDF/Word/图片等）作为输入源

**待实现：**
- [x] 解析用户文字描述，提取关键要求
- [x] 同时读取上传文件内容
- [x] 合并文字要求与文件内容，生成结构化大纲
- [ ] 确保两者信息不冲突、互补融合

---

## 2. PPT 风格与效果优化

**现状：** 生成的 PPT 风格单一，缺乏表格、图片等丰富元素

**目标：** 提升 PPT 视觉效果，增加多样化内容形式

**待实现：**
- [x] 添加多种 PPT 模板风格（学术、商务、简约等）
- [x] 支持表格生成（从数据自动生成格式化表格）
- [x] 支持图片插入：
  - [x] 用户上传的图片直接嵌入
  - [x] 从文件（如 PDF 截图）提取图片
  - [ ] 根据内容自动推荐/生成配图
- [x] 优化排版布局（标题、正文、图表的层次感）
- [x] 增加图表支持（柱状图、饼图、流程图等）

---

## 3. 新增布局类型（✅ 已完成 2026-03-12）

**新增 12+ 布局类型：**
- [x] three_col — 三栏并列
- [x] left_sidebar — 内容+左侧边栏
- [x] right_sidebar — 内容+右侧边栏
- [x] image_left — 图片左+文字右
- [x] image_right — 文字左+图片右
- [x] image_top — 图片上+文字下
- [x] quote — 引用页
- [x] center_focus — 居中聚焦
- [x] comparison — 对比矩阵
- [x] process — 流程图
- [x] chart — 图表

---

## 4. 视觉增强（✅ 已完成 2026-03-12）

**装饰元素：**
- [x] accent_bar — 侧边装饰条
- [x] corner_accent — 角落装饰
- [x] divider — 分割线
- [x] icon_bullets — 图标式要点

**色彩系统：**
- [x] 渐变背景支持
- [x] 每种风格的完整配色方案
- [x] 背景色调（background_tint）

**字体系统：**
- [x] 每种风格的独立字体配对
- [x] 标题/正文字号大小规范
- [x] 代码字体支持

**背景效果：**
- [x] 渐变填充
- [x] 图案叠加（简化版）
- [x] 侧边装饰条

---

## 5. 飞书卡片渲染优化（✅ 已完成 2026-03-12）

**问题：** 表格在飞书卡片中显示为原始 Markdown 文本而非渲染后的表格

**解决方案：**
- [x] 将 `sendMessage` 从 `msg_type: 'text'` 改为 `msg_type: 'interactive'`
- [x] 使用 `tag: 'markdown'` 元素替代 `tag: 'div'` + `tag: 'lark_md'`
- [x] 添加 `sendCard` MCP 工具支持
- [x] 在 IPC 层添加 `send_card` 消息类型处理
- [x] 在 router 层添加 `routeCardOutbound` 函数

**飞书 Markdown 限制说明：**
- 表格支持最多 10 列
- 每张卡片最多 5 个表格
- 只支持 H1 (`#`) 和 H2 (`##`) 标题
- 支持：粗体、斜体、代码、链接、列表、分割线

---

## 6. 剩余待办事项

**高级功能：**
- [ ] 自动图片推荐/生成（AI 驱动）
- [ ] 模板继承和自定义模板
- [ ] 动画效果和过渡
- [ ] 嵌入视频支持
- [ ] 高级图表类型（雷达图、树状图等）
